using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum GAMESTATES
{
    SCREENSAVER,
    DETERMINED,
    ENERGETIC,
    //GENEROUS,
    KINDNESS,
    PHOTOTAKING,

}

public enum ENERGETIC_PAGES
{
    INSTRUCTIONS,
    CALIBRATION,
    GAME,
    PASS_RESULT,
    FAIL_RESULT,
}

public enum DETERMINE_PAGES
{
    INSTRUCTIONS,
    CALIBRATION,
    GAME,
    PASS_RESULT,
    FAIL_RESULT,
}

public enum KINDNESS_PAGES
{
    INSTRUCTIONS,
    GAME,
    PASS_RESULT,
    FAIL_RESULT,
}

public enum GENEROUS_PAGES
{
    INSTRUCTIONS,
    DONATE,
    THANKYOU,
}

public enum PHOTOTAKING_PAGES
{
    PHOTOTAKE,
    REVIEW,
    //POSTPHOTO,
    //EMAIL,
    CONGRATS,
    THANKYOU,
}

// Add as needed
public enum PAGES
{
    SCREENSAVER,
    LOGIN,
    OTP,
    MAINMENU,
}


